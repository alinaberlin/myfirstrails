<!DOCTYPE html>
<html>
  <head>
    <title>Nameofapp</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>

    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  </head>

  <body>
  <nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Bike Berlin</a>
    </div>
    <ul class="nav navbar-nav">
      <li><%= link_to "Home", "/" %></li>
      <li><%= link_to "Products", products_url %></li>
      <li><%= link_to "About", static_pages_about_path %></li>
      <li><%= link_to "Contact", static_pages_contact_path %></li>
    </ul>
    <%= form_with(url: products_path, method: 'get', local: true, class: 'navbar-form navbar-right') do |form| %>
      <div class="form-group">
      <%= form.text_field :q, id: 'q', value: params[:q], class: "form-control" %>
      </div>
      <button type="submit" class="btn btn-default">
        <span class="glyphicon glyphicon-search"></span>
      </button>     
    <% end %>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
      <li><a rel="nofollow" data-method="delete" href="/logout"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>
      <% else %>
      <li><a rel="nofollow" href="/sign_up"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
      <li><a rel="nofollow" href="/login"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
      <% end %>
    </ul>
  </div>
</nav>  


<!-- Flash Notice Tags -->
<% if notice %>
    <p id="notice" class="alert alert-info"><%= notice %></p>
<% end %>
<% if alert %>
    <p id="alert" class="alert alert-info"><span class="glyphicon glyphicon-warning-sign"></span><%= alert %></p>
<% end %>

<%= yield %>

<footer>
  <div class="container"><p class="text-muted">&copy; 2017 Bike Berlin</p></div>
</footer>
</body>
</html>
